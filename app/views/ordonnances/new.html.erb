<h1> <%=@title%> </h1>
<%= form_for @ordonnance, :validate => true  do |f| %>
  <%= render 'fields', :f=>f%>
  <div class="actions">
    <%= f.submit "Créer", :class => "button" %>
  </div>
<% end %>

