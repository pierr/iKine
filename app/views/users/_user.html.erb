<li>
  <%= gravatar_for user, :size => 30 %>


<!-- TRES GROSSE ERREUR : en faisant ça, on change le nom du gars en "nom prenom".-->
<!%= link_to user.nom.concat(" ").concat(user.prenom), user %>
<!--La correction est ci-dessous. -->
 <%= link_to  ( user.nom + " " + user.prenom ) , user %>






  <% if current_user.admin? %>
	| <%= link_to "delete", user, :method => :delete, :confirm => "You sure?",
                              	  :title => "Delete #{user.nom} #{user.prenom}" %>
  <% end %>
</li>