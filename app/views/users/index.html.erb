<h1>Tous les utilisateurs</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, :size => 30 %>
      <%= link_to user.nom, user %> <%= link_to user.prenom, user %>

    </li>
  <% end %>
</ul>
