<h1>Modifier user</h1>

<%= form_for @user, :validate => true do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit "Enregistrer" %>
  </div>
<% end %>

<div>
  <%= gravatar_for @user %>
  <a href="http://gravatar.com/emails">change</a>
</div>
