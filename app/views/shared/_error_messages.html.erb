<% if object.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(object.errors.count, "erreur") %> 
        empèche(nt) cet  <%= object.class.to_s.underscore.humanize.downcase %> 
        d'être enregistré :</h2>
    <p>Il y a des problèmes avec les champs suivants:</p>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
